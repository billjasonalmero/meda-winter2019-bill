//Use the Express package.
const express = require("express");

//Run the express
const app = express();

//Feed Express server
const http = require();

const port = 3000;

//Tell HTTP which port
http.liten(port);

console.log("The express server is running on port " + port + "!")

